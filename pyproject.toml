[project]
name = "agent-ecoradar"
version = "0.1.0"
description = "Reusable AI Coding Agent ecosystem tracker (Agent EcoRadar)"
requires-python = ">=3.12"
dependencies = []

[tool.agent-ecoradar]
data_format = "json-compatible-yaml"
